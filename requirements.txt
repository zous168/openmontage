# OpenMontage 的依赖已并入 agent-hub/pyproject.toml。
#
# 合并成单一仓库、单一运行环境后，两份依赖清单必然漂移，
# 所以这里不再列具体包，只留指路。
#
#   安装：cd agent-hub && uv sync
#   或：  pip install -e agent-hub          （若已配置 build-system）
#   GPU： pip install "torch>=2.0" "torchaudio>=2.0" "torchvision>=0.15"
#         对应 pyproject 的 [project.optional-dependencies] gpu 组；
#         刻意不进主依赖 —— 体积上千 MB，且不装时相关工具会经
#         BaseTool.get_status() 优雅降级为 UNAVAILABLE。
#
# SSOT: agent-hub/pyproject.toml
